export class ArticleInfo {
    id: string;
    name: string;
    purchasePrice: number;
    sellPrice: number;
    currentStock: number;
    minDesiredStock: number;
    maxDesiredStock: number;
    taxRate: number;
}